# NOTE: model/ (including the .gguf) must stay in the build context; app.py loads it at startup.

# Git
.git/
.gitignore
.dockerignore

# Generated by app.py; rebuilt inside the container on first run
faiss_index.faiss
chunks_metadata.json

# Python virtual environment / cache
rag/
.venv/
venv/
__pycache__/
*.py[cod]

# Docs and screenshots (not needed at runtime)
README.md
image/

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
